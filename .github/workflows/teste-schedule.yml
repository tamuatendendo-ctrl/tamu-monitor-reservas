name: TESTE AGENDAMENTO TAMU

on:
  schedule:
    - cron: "47 * * * *"
  workflow_dispatch:

jobs:
  teste:
    runs-on: ubuntu-latest

    steps:
      - name: Confirmar agendamento
        run: |
          echo "======================================"
          echo "TESTE DE AGENDAMENTO TAMU"
          echo "======================================"
          echo "O GitHub acionou este workflow!"
          echo "Data/hora UTC:"
          date -u
          echo "======================================"
